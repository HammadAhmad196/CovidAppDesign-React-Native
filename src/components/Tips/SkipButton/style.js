import { StyleSheet } from 'react-native';

const style = StyleSheet.create ({
    container: {
        backgroundColor: '#FFF',
        borderRadius: 15,
        padding: 7,
        marginHorizontal: 10
    },
    title: {
        fontSize: 18,
        fontWeight: 'bold'
    }
});
export default style;